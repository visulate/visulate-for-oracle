#!/bin/bash
base_url=https://{{host}}/api/{{params.db}}/{{params.owner}}/{{params.type}}

echo Generating SQL*Loader control files
{{#each results}}

echo {{this}}.ctl
curl "$base_url"/{{this}}?template=gen_sql_loader_control_file.hbs -o {{this}}.ctl
{{/each}}