import { Deserializable.Model } from './deserializable.model';

describe('Deserializable.Model', () => {
  it('should create an instance', () => {
    expect(new Deserializable.Model()).toBeTruthy();
  });
});
