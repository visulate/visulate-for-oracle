import { CurrentContext.Model } from './current-context.model';

describe('CurrentContext.Model', () => {
  it('should create an instance', () => {
    expect(new CurrentContext.Model()).toBeTruthy();
  });
});
