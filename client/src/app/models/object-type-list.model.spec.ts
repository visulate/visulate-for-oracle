import { ObjectTypeListItem } from './object-type-list.model';

describe('ObjectTypeListItem', () => {
  it('should create an instance', () => {
    expect(new ObjectTypeListItem()).toBeTruthy();
  });
});
